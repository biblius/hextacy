### STORAGE ###

# Postgres
PG_USER = "postgres"
PG_PASSWORD = "postgres"
PG_HOST = "localhost"
PG_DATABASE = "myco_test_db"
PG_POOL_SIZE = 8
PG_PORT = 5432
POSTGRES_URL = "postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"

# Redis

RD_USER = "default"
RD_PASSWORD = "d35b6af8"
RD_HOST = "localhost"
RD_DATABASE = 0
RD_POOL_SIZE = 8 
RD_PORT = 6379
REDIS_URL = "redis://${RD_HOST}"

# Mongo

MONGO_APP_NAME = "myco test suite"
MONGO_USER = "admin"
MONGO_PASSWORD = "password"
MONGO_DATABASE = "myco_test"
MONGO_AUTH_DB = "admin"
MONGO_HOST = "localhost"
MONGO_PORT = 27017
MONGO_URL = "mongodb://${MONGO_USER}:{$MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DATABASE}?authSource=${MONGO_AUTH_DB}"